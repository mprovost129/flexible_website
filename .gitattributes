# Files and directories marked export-ignore are excluded from `git archive`
# (i.e. the ZIP you send to testers via `git archive --format=zip HEAD`).

# Sandbox demo — not part of the distributable product
sandbox/                    export-ignore
templates/sandbox/          export-ignore
static/sandbox/             export-ignore
